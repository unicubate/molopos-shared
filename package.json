{
    "name": "shared",
    "version": "2.1.5",
    "scripts": {
        "dev": "tsc --watch",
        "docs": "node ./scripts/docs .js",
        "test": "bun ./src/lib/codice-fiscale/test.ts",
        "build": "rm -rf ./dist && tsc && node ./scripts/package.js"
    }
}
