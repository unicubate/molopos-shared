{
    "name": "shared",
    "version": "2.0.3",
    "scripts": {
        "dev": "tsc --watch",
        "docs": "node ./scripts/readme.js",
        "build": "rm -rf ./dist && tsc && node ./scripts/package.js"
    }
}
